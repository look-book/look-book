import React from 'react';

function Splash() {
    return (
        <section>
            <h1>Splash</h1>
            <p>This page will contain the logo and be the first page someone sees when they open the app.</p>
        </section>
    )
}

export default Splash;